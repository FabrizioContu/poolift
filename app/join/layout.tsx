import type { Metadata } from "next";

export const metadata: Metadata = { title: "Unirse a Grupo | Poolift" };

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

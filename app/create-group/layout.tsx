import type { Metadata } from "next";

export const metadata: Metadata = { title: "Crear Grupo | Poolift" };

export default function CreateGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
